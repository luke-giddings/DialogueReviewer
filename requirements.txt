# Runtime dependencies for Dialogue Reviewer.
# Unpinned during early development; run `pip freeze > requirements.txt`
# once your venv is stable to lock exact versions before publishing.

anthropic
requests
pandas
pandas-stubs
openpyxl
python-dotenv
jinja2
ruff

